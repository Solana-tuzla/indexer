import { BlockParser } from "./BlockParser";


describe('Block Parser test suite', () => {

    // Test #1 - Check if we can call the dummy method
    it('Calls the dummy method of the BlockParser class', () => {
        const somethingElse = BlockParser.dummyMethod();
        expect(somethingElse).toEqual('Something else');
    });

    // Test #2

    // Test #3 

    // ...

});
